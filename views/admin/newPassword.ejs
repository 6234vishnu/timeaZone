<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background-color: #f4f4f4;
        }

        .header {
            width: 100%;
            text-align: left;
            padding: 20px;
            background-color: #fff;
        }

        .header img {
            max-width: 150px;
            height: auto;
        }

        .banner {
            width: 100%;
            max-height: 250px;
            overflow: hidden;
            margin-bottom: 20px;
        }

        .banner img {
            width: 100%;
            height: auto;
            display: block;
        }

        .form-container {
            width: 90%;
            max-width: 400px;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        .form-container h2 {
            margin-bottom: 20px;
            font-size: 24px;
            color: #333;
            text-align: center;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            color: #555;
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }

        .btn {
            width: 100%;
            padding: 10px;
            background-color: #333;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            font-weight: bold;
            text-transform: uppercase;
        }

        .btn:hover {
            background-color: #555;
        }

        @media (max-width: 600px) {
            .header img {
                max-width: 120px;
            }

            .form-container {
                padding: 15px;
            }

            .form-group input {
                padding: 8px;
            }

            .btn {
                padding: 8px;
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <img src="\images\timezone.png" alt="Time Zone Logo">
    </header>
    <div class="form-container">
        <h2>Password Reset</h2>
        <form action="/newPasswordAdmin" method="post">
            <div class="form-group">
                <label for="new-password">Enter New Password</label>
                <input type="password" id="new-password" placeholder="Enter New Password" name="newPassword"  required minlength="8"  pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}">
            </div>
            <div class="form-group">
                <label for="confirm-password">Confirm Password</label>
                <input type="password" id="confirm-password" placeholder="Confirm Password" name="confirmPassword">
            </div>
            <div id="error5" class="error-message"></div>
            <button type="submit" class="btn">Submit</button>
        </form>
    </div>
    <script>
          const passwordid = document.getElementById('new-password');
          const confirmid = document.getElementById('confirm-password');

          signform.addEventListener('submit', function(event) {
            let valid = true;

            const passwordPattern = /^(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}$/;
            if (newPasswordid.value.trim() === "") {
        error6.textContent = "Please enter a new password.";
        valid = false;
    } else if (!passwordPattern.test(newPasswordid.value.trim())) {
        error6.textContent = "New password must be at least 8 characters long and include at least one uppercase letter, one lowercase letter, and one number.";
        valid = false;
    } else if (newPasswordid.value.trim() !== confirmNewPasswordid.value.trim()) {
        error6.textContent = "New passwords do not match.";
        valid = false;
    } else {
        error6.textContent = "";
    }

    
    if (confirmNewPasswordid.value.trim() === "") {
        error7.textContent = "Please confirm your new password.";
        valid = false;
    } else if (confirmNewPasswordid.value.trim() !== newPasswordid.value.trim()) {
        error7.textContent = "New passwords do not match.";
        valid = false;
    } else {
        error7.textContent = "";
    }

    if (!valid) {
        event.preventDefault(); 
    }

      }) 
    </script>

</body>
</html>
